$(document).ready(function () {
    // El depósito ahora lo maneja Python mediante un formulario POST.
    // Solo dejamos esto por si en el futuro quieres agregar alguna alerta visual,
    // pero por ahora, Django se encarga de todo el proceso.
});